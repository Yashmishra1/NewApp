import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 30,
  },
  listings: {
    padding: 16,
  },
  image: {
    width: '100%',
    height: 300,
    borderRadius: ms(10),
  },
});
